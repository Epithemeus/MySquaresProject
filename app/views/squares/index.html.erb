<h1>Listing squares</h1>

<table>
  <tr>
    <th>Length</th>
    <th>Width</th>
  </tr>

  <% @squares.each do |square| %>
    <tr>
      <td><%= square.width %></td>
      <td><%= square.length %></td>
    </tr>
  <% end %>
</table>
