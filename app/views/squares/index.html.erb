<h1>Listing squares</h1>

<table>
  <tr>
    <th>Length</th>
    <th>Width</th>
  </tr>

  <% @squares.each do |square| %>
    <tr>
      <td><%= square.width %></td>
      <td><%= square.length %></td>
      <td><%= link_to 'Edit', edit_square_path(square) %></td>
    </tr>
  <% end %>
</table>
